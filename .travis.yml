language: cpp

compiler:
  - clang
  - gcc

sudo: required

install: ./setup-travis.sh

script:
  - mkdir build
  - cd build
  - cmake ..
  - make
  - make test

branches:
  only:
    - master

os:
  - linux
  - osx