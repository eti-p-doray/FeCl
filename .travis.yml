language: cpp
compiler:
  - clang
  - gcc

sudo: required
before_install: 
  - chmod +x ./setup-travis.sh
  - sudo ./setup-travis.sh
before_script:
  - mkdir build
  - cd build
  - cmake ..
script:
  - make
  - make test

branches:
  only:
    - master

os:
  - linux
  - osx